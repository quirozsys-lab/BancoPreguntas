@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        var uri = Uri.EscapeDataString(Nav.Uri);
        Nav.NavigateTo($"/Account/Login?returnUrl={uri}", forceLoad: true);
    }
}
